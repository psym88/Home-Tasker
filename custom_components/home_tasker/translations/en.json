{
  "config": {
    "step": {"user": {"title": "Set up Home Tasker", "description": "Creates the local Home Tasker configuration."}},
    "abort": {"already_configured": "Home Tasker is already configured."}
  }
}
